{
  "name": "subgraph-block",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ subgraph-block-rinkeby",
    "create-local": "graph create --node http://localhost:8020/ subgraph-block-rinkeby",
    "remove-local": "graph remove --node http://localhost:8020/ subgraph-block-rinkeby",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 subgraph-block-rinkeby"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
